#include "Contact.h"#include <iomanip>#include <iostream>using namespace std;/// @brief Default constructor that initializes all contact fields with empty valuesContact::Contact() : id(0),                     contactType(PERSON),                     name(""),                     phone(""),                     email(""),                     street(""),                     city(""),                     state(""),                     zipCode(""),                     group(""),                     tags(),                     companyName(""),                     jobTitle(""),                     department(""),                     serviceType(""),                     accountNumber(""),                     relationship(""),                     birthday(""),                     emergencyPriority(""),                     notes("") {}/// @brief Constructor that initializes a Contact with basic information and empty optional fieldsContact::Contact(int newId,                 ContactType newContactType,                 string newName,                 string newPhone,                 string newEmail)        : id(newId),          contactType(newContactType),          name(newName),          phone(newPhone),          email(newEmail),          street(""),          city(""),          state(""),          zipCode(""),          group(""),          tags(),          companyName(""),          jobTitle(""),          department(""),          serviceType(""),          accountNumber(""),          relationship(""),          birthday(""),          emergencyPriority(""),          notes("") {}/// @brief Returns the contact's unique identifierint Contact::getId() const {    return id;}/// @brief Sets the contact's unique identifiervoid Contact::setId(int newId) {    id = newId;}/// @brief Returns the contact's typeContactType Contact::getContactType() const {    return contactType;}/// @brief Sets the contact's typevoid Contact::setContactType(ContactType newContactType) {    contactType = newContactType;}/// @brief Returns the contact's namestring Contact::getName() const {    return name;}/// @brief Sets the contact's namevoid Contact::setName(string newName) {    name = newName;}/// @brief Returns the contact's phone numberstring Contact::getPhone() const {    return phone;}/// @brief Sets the contact's phone numbervoid Contact::setPhone(string newPhone) {    phone = newPhone;}/// @brief Returns the contact's email addressstring Contact::getEmail() const {    return email;}/// @brief Sets the contact's email addressvoid Contact::setEmail(string newEmail) {    email = newEmail;}/// @brief Returns the contact's street addressstring Contact::getStreet() const {    return street;}/// @brief Sets the contact's street addressvoid Contact::setStreet(string newStreet) {    street = newStreet;}/// @brief Returns the contact's citystring Contact::getCity() const {    return city;}/// @brief Sets the contact's cityvoid Contact::setCity(string newCity) {    city = newCity;}/// @brief Returns the contact's statestring Contact::getState() const {    return state;}/// @brief Sets the contact's statevoid Contact::setState(string newState) {    state = newState;}/// @brief Returns the contact's zip codestring Contact::getZipCode() const {    return zipCode;}/// @brief Sets the contact's zip codevoid Contact::setZipCode(string newZipCode) {    zipCode = newZipCode;}/// @brief Returns the contact's group namestring Contact::getGroup() const {    return group;}/// @brief Sets the contact's group namevoid Contact::setGroup(string newGroup) {    group = newGroup;}/// @brief Returns all tags assigned to the contactvector<string> Contact::getTags() const {    return tags;}/// @brief Replaces all tags with the provided listvoid Contact::setTags(vector<string> newTags) {    tags = newTags;}/// @brief Adds a single tag to the contactvoid Contact::addTag(string newTag) {    tags.push_back(newTag);}/// @brief Removes a tag from the contactvoid Contact::removeTag(string tagToRemove) {    for (int i = 0; i < tags.size(); i++) {        if (tags[i] == tagToRemove) {            tags.erase(tags.begin() + i);            return;        }    }}/// @brief Returns the contact's company namestring Contact::getCompanyName() const {    return companyName;}/// @brief Sets the contact's company namevoid Contact::setCompanyName(string newCompanyName) {    companyName = newCompanyName;}/// @brief Returns the contact's job titlestring Contact::getJobTitle() const {    return jobTitle;}/// @brief Sets the contact's job titlevoid Contact::setJobTitle(string newJobTitle) {    jobTitle = newJobTitle;}/// @brief Returns the contact's departmentstring Contact::getDepartment() const {    return department;}/// @brief Sets the contact's departmentvoid Contact::setDepartment(string newDepartment) {    department = newDepartment;}/// @brief Returns the contact's service typestring Contact::getServiceType() const {    return serviceType;}/// @brief Sets the contact's service typevoid Contact::setServiceType(string newServiceType) {    serviceType = newServiceType;}/// @brief Returns the contact's account numberstring Contact::getAccountNumber() const {    return accountNumber;}/// @brief Sets the contact's account numbervoid Contact::setAccountNumber(string newAccountNumber) {    accountNumber = newAccountNumber;}/// @brief Returns the contact's relationship informationstring Contact::getRelationship() const {    return relationship;}/// @brief Sets the contact's relationship informationvoid Contact::setRelationship(string newRelationship) {    relationship = newRelationship;}/// @brief Returns the contact's birthdaystring Contact::getBirthday() const {    return birthday;}/// @brief Sets the contact's birthdayvoid Contact::setBirthday(string newBirthday) {    birthday = newBirthday;}/// @brief Returns the contact's emergency priority levelstring Contact::getEmergencyPriority() const {    return emergencyPriority;}/// @brief Sets the contact's emergency priority levelvoid Contact::setEmergencyPriority(string newEmergencyPriority) {    emergencyPriority = newEmergencyPriority;}/// @brief Returns additional notes about the contactstring Contact::getNotes() const {    return notes;}/// @brief Sets additional notes about the contactvoid Contact::setNotes(string newNotes) {    notes = newNotes;}/// @brief Converts the contact type to a readable string formatstring Contact::getContactTypeString() const{    switch (contactType)    {    case PERSON:        return "Person";    case BUSINESS:        return "Business";    case VENDOR:        return "Vendor";    case EMERGENCY:        return "Emergency";    default:        return "Unknown";    }}/// @brief Prints all contact information to the console in a formatted, easy-to-read layoutvoid Contact::displayContact() const{    const int labelWidth = 18;    cout << left << setw(labelWidth) << "ID" << " : " << id << endl;    cout << left << setw(labelWidth) << "Type" << " : " << getContactTypeString() << endl;    if (name != "")        cout << left << setw(labelWidth) << "Name" << " : " << name << endl;    if (phone != "")        cout << left << setw(labelWidth) << "Phone" << " : " << phone << endl;    if (email != "")        cout << left << setw(labelWidth) << "Email" << " : " << email << endl;    if (street != "")        cout << left << setw(labelWidth) << "Street" << " : " << street << endl;    if (city != "")        cout << left << setw(labelWidth) << "City" << " : " << city << endl;    if (state != "")        cout << left << setw(labelWidth) << "State" << " : " << state << endl;    if (zipCode != "")        cout << left << setw(labelWidth) << "Zip Code" << " : " << zipCode << endl;    if (group != "")        cout << left << setw(labelWidth) << "Group" << " : " << group << endl;    if (!tags.empty())    {        cout << left << setw(labelWidth) << "Tags" << " : ";        for (int i = 0; i < tags.size(); i++)        {            cout << tags[i];            if (i < tags.size() - 1)                cout << ", ";        }        cout << endl;    }    if (companyName != "")        cout << left << setw(labelWidth) << "Company Name" << " : " << companyName << endl;    if (jobTitle != "")        cout << left << setw(labelWidth) << "Job Title" << " : " << jobTitle << endl;    if (department != "")        cout << left << setw(labelWidth) << "Department" << " : " << department << endl;    if (serviceType != "")        cout << left << setw(labelWidth) << "Service Type" << " : " << serviceType << endl;    if (accountNumber != "")        cout << left << setw(labelWidth) << "Account Number" << " : " << accountNumber << endl;    if (relationship != "")        cout << left << setw(labelWidth) << "Relationship" << " : " << relationship << endl;    if (birthday != "")        cout << left << setw(labelWidth) << "Birthday" << " : " << birthday << endl;    if (emergencyPriority != "")        cout << left << setw(labelWidth) << "Emergency Priority" << " : " << emergencyPriority << endl;    if (notes != "")        cout << left << setw(labelWidth) << "Notes" << " : " << notes << endl;}/// @brief Returns all tags as a single string with semicolons between themstring Contact::getTagsAsString() const {    string result;    for (int i = 0; i < tags.size(); i++) {        result += tags[i];        if (i < tags.size() - 1) {            result += ";";        }    }    return result;}